export type columnType = {
    label: string,
    size: string | number,
    sortable: boolean,
    value: string
}
export type TableRecord = {
    id: string,
    isSelected: boolean,
    data: any
}
